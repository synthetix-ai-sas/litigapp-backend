services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: litigapp
      POSTGRES_USER: litigapp
      POSTGRES_PASSWORD: litigapp_dev
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U litigapp -d litigapp"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
